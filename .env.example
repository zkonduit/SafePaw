# SafeClaw Environment Configuration

# Server Configuration
PORT=12345
HOST=0.0.0.0
NODE_ENV=development

# Ethereum Configuration
# Set to 'true' to start embedded Anvil, or 'false' to use external RPC
# The start.sh script automatically starts Anvil on the host machine
START_ANVIL=false

# RPC endpoint
# For local development: http://127.0.0.1:8545 (Anvil runs on host machine)
# For Docker: http://host.docker.internal:8545 (configured in docker-compose.yml)
ETHEREUM_RPC_URL=http://127.0.0.1:8545

# Public key/address (used by safeclaw.sh script when agent ID not specified)
# This is Anvil's first default account address
PUBLIC_KEY=0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266

# Private key for signing transactions (REQUIRED)
# This is Anvil's first default account private key - DO NOT use in production
PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# Contract address (automatically set after deployment)
# Leave empty to deploy a new contract on startup
CONTRACT_ADDRESS=
